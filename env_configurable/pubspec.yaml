name: env_configurable
description: Generate class with properties from environment variables..
version: 3.3.2

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  build_runner: any
  source_gen: any
  recase: any

  env_annotation:
    git:
      url: https://github.com/Hecatoncheir/env_configurable.dart.git
      path: env_annotation

dev_dependencies:
  build_test: any
  test: any

dependency_overrides:
  analyzer: ^8.1.1
